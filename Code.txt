Perfect, bro. Here's the **exact command sequence** to **clone** and **install Node.js v22 offline** from your GitHub:

---

## ✅ Offline Node.js Install From Your GitHub Zip

### 📍 1. **Clone Your Repo**

```bash
git clone https://github.com/DivyanshSinghPariharCodeForFun/do.git
cd do
```

---

### 📍 2. **Unzip the Node Bundle**

```bash
unzip node-bundle.zip
```

This gives you the folder:

```bash
node-v22.2.0-linux-x64/
```

---

### 📍 3. **Move Node to Global Path**

```bash
sudo mv node-v22.2.0-linux-x64 /opt/nodejs
```

---

### 📍 4. **Add Node to Your PATH**

```bash
echo 'export PATH=/opt/nodejs/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
```

---

### 📍 5. **Verify Installation**

```bash
node -v
npm -v
```

You should see:

```bash
v22.2.0
10.x.x or whatever npm version bundled
```

---

## 💡 Optional: Make Global for All Users

If you want this Node.js version available system-wide:

```bash
sudo ln -s /opt/nodejs/bin/node /usr/bin/node
sudo ln -s /opt/nodejs/bin/npm /usr/bin/npm
```

---

You're locked and loaded. If you want I can give you an all-in-one shell script (`install-node-offline.sh`) to automate this process on any Ubuntu server. Want that?
